# 📘 دليل مشروع "Zusammen Umzüge" (معاً للنقل)

هذا الملف هو دليلك الشامل لفهم كيفية عمل الموقع، أين تجد الملفات المهمة، وكيف تقوم بالتعديلات الأساسية. تم إعداده ليوفر لك وللعميل مرجعاً سهلاً وواضحاً باللغة العربية.

---

## 1. المميزات الجديدة (ماذا أضفنا؟) ✨

تم تحديث الموقع ليشمل نظام إدارة متكامل ومولد فواتير احترافي:

### 🔹 لوحة تحكم المدير (Admin Panel)
- **الدخول:** عبر الرابط `/login` (مثلاً `zusammen-umzuege.de/login`).
- **المميزات:**
    - مشاهدة جميع طلبات عروض السعر (Anfragen) الواردة من العملاء.
    - تفاصيل كل طلب (العنوان، العفش، التاريخ، إلخ).
    - **تحويل الطلب إلى فاتورة بضغطة زر واحد.**

### 🔹 مولد الفواتير (Invoice Generator)
- **المكان:** `/invoice` أو عبر لوحة التحكم.
- **المميزات:**
    - تصميم فاتورة ألماني قياسي (A4) جاهز للطباعة.
    - **تعبئة تلقائية:** عند الدخول من طلب عميل، يتم ملء الاسم والعنوان والخدمات تلقائياً.
    - **تعديل حر:** يمكنك إضافة بنود، تغيير الأسعار، أو تعديل البيانات مباشرة على الشاشة.
    - **الطباعة:** زر "طباعة" يقوم بتحويل الفاتورة لملف PDF جاهز للإرسال أو الطباعة.

---

## 2. هيكلية المجلدات والأكواد (أين أجد ملفاتي؟) 📂

المشروع مبني بأحدث التقنيات: **Next.js** (للسرعة والأداء)، **Tailwind CSS** (للتصميم)، و **MongoDB** (لقاعدة البيانات).

### `src/app/[locale]/` 🌍 (الصفحات الرئيسية)
هنا تعيش جميع صفحات الموقع. المجلد `[locale]` يعني أن الصفحات تدعم تعدد اللغات (ar, de, en) تلقائياً.
- **`page.tsx`**: الصفحة الرئيسية (Home).
- **`layout.tsx`**: التصميم العام (الهيكل) الذي يحتوي على القائمة (Header) والتذييل (Footer).
- **`admin/`**: مجلد لوحة تحكم المدير (محمية بكلمة مرور).
- **`invoice/`**: صفحة مولد الفواتير.
- **`login/`**: صفحة تسجيل الدخول.

### `src/components/` 🧩 (قطع البناء)
يحتوي على الأجزاء التي نستخدمها لبناء الصفحات:
- **`InvoiceGenerator.tsx`**: الكود المسؤول عن شكل وعمل الفاتورة.
- **`header.tsx`**: الشريط العلوي (النافبار).
- **`footer.tsx`**: تذييل الصفحة (الشعار، الروابط، حقوق النشر).
- **`QuoteFormFull.tsx`**: نموذج طلب عرض السعر الطويل.

### `src/messages/` 🗣️ (مركز النصوص والترجمة)
**هذا أهم مجلد للمحتوى!** كل كلمة تظهر في الموقع موجودة هنا.
- **`de.json`**: النصوص باللغة الألمانية (الأساسية).
- **`ar.json`**: النصوص باللغة العربية.
- **`en.json`**: النصوص باللغة الإنجليزية.
> **💡 تلميح:** لتغيير نص، سعر، أو رقم هاتف، عدل هذه الملفات.

---

## 3. سيناريو العمل الكامل (Workflow) ⚙️

1.  **العميل يقدم طلب:**
    - يدخل الزائر للموقع ويملأ نموذج "طلب عرض سعر".
    - البيانات تُحفظ فوراً في قاعدة البيانات.

2.  **المدير يراجع الطلب:**
    - تدخل أنت (المدير) إلى لوحة التحكم (`/admin`).
    - ترى الطلب الجديد في القائمة.
    - تضغط على "Details" لمشاهدة التفاصيل.

3.  **إنشاء الفاتورة:**
    - في صفحة تفاصيل الطلب، تضغط على زر **"Create Invoice"**.
    - يفتح النظام صفحة الفاتورة وقد تم ملء بيانات العميل والعفش تلقائياً.
    - تراجع الأسعار، تضيف أي خدمات إضافية، ثم تضغط **"Print"** لحفظها كـ PDF وإرسالها للعميل.

---

## 4. خطوات التعديل الشائعة (للمدير) ✏️

### لتعديل بيانات الشركة في الفاتورة:
1.  افتح ملف `src/components/InvoiceGenerator.tsx`.
2.  ابحث عن دالة `loadRequestData` أو القيم الافتراضية (State).
3.  يمكنك تعديل العنوان الافتراضي، الرقم الضريبي، واسم المالك هناك ليظهر دائماً بشكل صحيح.

### لتغيير الأسعار في الموقع (مثلاً سعر الساعة):
1.  افتح `src/messages/de.json`.
2.  ابحث عن `"PricingInfo"`.
3.  غير القيمة، وكرر الأمر في الملفات (`ar.json`, `en.json`).

### لتغيير الشعار (Logo):
1.  ضع صورة الشعار الجديد في مجلد `public/`.
2.  تأكد أن اسمها مطابق للاسم في الكود (`logo.png` أو `Logo-Umzüge.png`).

---

## 5. النشر والتحديث (GitHub & Netlify) 🚀

عندما نقوم بأي تعديلات ونريد رفعها للموقع الحي:
1.  نحفظ التغييرات: `git add .` و `git commit`.
2.  نرفعها لـ GitHub: `git push`.
3.  **Netlify**: سيقوم تلقائياً باكتشاف التغييرات الجديدة وبناء الموقع ونشره في غضون دقائق. لا حاجة لتدخل يدوي!

---

**ملاحظة:** هذا المشروع مصمم ليكون **سريعاً جداً**، متجاوباً مع الموبايل، ويدعم SEO بشكل ممتاز لمحركات البحث.

بالتوفيق في مشروعك! ��
