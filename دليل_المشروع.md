# 📘 دليل مشروع "Zusammen Umzüge" (معاً للنقل)

هذا الملف هو دليلك الشامل لفهم كيفية عمل الموقع، أين تجد الملفات المهمة، وكيف تقوم بالتعديلات الأساسية. تم إعداده ليكون مرجعاً سهلاً لك وللعميل.

---

## 1. هيكلية المجلدات والأكواد (أين أجد ملفاتي؟) 📂

المشروع مبني بأحدث التقنيات: **Next.js** (للسرعة والأداء)، **Tailwind CSS** (للتصميم)، و **MongoDB** (لقاعدة البيانات)، مع نظام تعدد لغات متطور (**next-intl**).

### `src/app/[locale]/` 🌍 (الصفحات الرئيسية)
هنا تعيش جميع صفحات الموقع. المجلد `[locale]` يعني أن الصفحات تدعم تعدد اللغات (ar, de, en) تلقائياً.
- **`page.tsx`**: الصفحة الرئيسية (Home).
- **`layout.tsx`**: التصميم العام (الهيكل) الذي يحتوي على النافبار (Header) والفوتر (Footer) والخطوط.
- **`services/page.tsx`**: صفحة "خدماتنا" (Leistungen).
- **`angebot/page.tsx`**: صفحة "طلب عرض سعر" (Anfrage).
- **`kontakt/page.tsx`**: صفحة "اتصل بنا" (Kontakt).
- **`login/`**: صفحة دخول الأدمن (الإدارة).

### `src/components/` 🧩 (قطع البناء)
يحتوي على الأجزاء التي نستخدمها لبناء الصفحات:
- **`Header.tsx`**: الشريط العلوي (النافبار - القائمة).
- **`Footer.tsx`**: تذييل الصفحة (يحتوي على الشعار، الروابط، وحقوق النشر).
- **`QuoteFormFull.tsx`**: نموذج طلب عرض السعر الكبير والمفصل.
- **`InvoiceGenerator.tsx`**: أداة إنشاء الفواتير (للاستخدام الداخلي أو للعميل).
- **`PricingAndDetails.tsx`**: الجزء الذي يعرض قائمة الأسعار (50€، 20€...) وقائمة الخدمات التفصيلية.
- **`ServiceCards.tsx`**: البطاقات التي تعرض الخدمات باختصار (نقل، تنظيف، إلخ).

### `src/messages/` 🗣️ (مركز النصوص والترجمة)
**هذا أهم مجلد للمحتوى!** كل كلمة تظهر في الموقع موجودة هنا وليست في ملفات الكود.
- **`de.json`**: النصوص باللغة الألمانية (الرئيسية).
- **`ar.json`**: النصوص باللغة العربية.
- **`en.json`**: النصوص باللغة الإنجليزية.
> **💡 تلميح:** إذا أردت تغيير نص، سعر، أو رقم هاتف، غالباً ستجده داخل هذه الملفات.

### `src/lib/` & `src/models/` 🧠 (العقل والمنطق)
- **`mail.ts`**: المسؤول عن إرسال الإيميلات (حالياً في وضع المحاكاة Mock Mode).
- **`db.ts`**: إعدادات الاتصال بقاعدة البيانات MongoDB.
- **`models/Request.ts`**: شكل البيانات التي يتم حفظها عند طلب عرض سعر.

---

## 2. كيف يعمل الموقع؟ (السيناريوهات الهامة) ⚙️

### أ. كيف تعمل تعدد اللغات؟
الموقع يقرأ اللغة من الرابط (مثلاً `/de/services` أو `/ar/services`).
بناءً على اللغة، يقوم الموقع بجلب النصوص المناسبة من ملفات `messages/*.json`.
- لهذا السبب، عند تعديل الأسعار، يجب تعديلها في الملفات الثلاثة (`ar.json`, `de.json`, `en.json`) لضمان ظهورها بشكل صحيح لكل الزوار.

### ب. دورة حياة "طلب عرض السعر" (Angebot)
1.  يدخل العميل لصفحة "طلب عرض سعر".
2.  يملأ النموذج (من أين، إلى أين، العفش، ...).
3.  عند الضغط على "إرسال":
    *   يتم حفظ الطلب في قاعدة البيانات (MongoDB).
    *   يتم إرسال بريد إلكتروني للإدارة بتفاصيل الطلب (يتم تفعيله عند ربط الدومين الحقيقي).

### ج. الفاتورة والأدوات
يحتوي الموقع على **Invoice Generator** مدمج يعمل داخل المتصفح، يسمح لك بإنشاء فواتير PDF احترافية بسرعة دون الحاجة لبرامج محاسبة معقدة، وهو مصمم ليكون ديناميكياً (تكتب البيانات ويتم الحساب تلقائياً).

---

## 3. خطوات التعديل الشائعة (للمدير/المالك) ✏️

### 1- تعديل الأسعار (مثلاً تغيير الـ 50€) 💶
1.  افتح المجلد `src/messages/`.
2.  افتح ملف `de.json`.
3.  ابحث عن قسم `"PricingInfo"`.
4.  ستجد: `"basePrice": "50€ / Stunde"`. قم بتغيير الرقم.
5.  كرر العملية في `ar.json` و `en.json`.

### 2- تعديل رقم الهاتف أو الإيميل 📞
1.  افتح `src/messages/de.json`.
2.  ابحث عن الرقم الحالي أو الإيميل.
3.  عدله وسيتغير تلقائياً في كل مكان يستخدم هذا المفتاح (الفوتر، صفحة الاتصال، الهيدر).

### 3- تغيير الصور (الشعار مثلاً) 🖼️
1.  الصور موجودة في المجلد الرئيسي `public/`.
2.  لاستبدال الشعار، احذف `logo-Circle.png` وضع ملفك الجديد **بنفس الاسم** تماماً.
3.  أو قم بتغيير الاسم في الكود (`Header.tsx` و `Footer.tsx`) ليشير لاسم الملف الجديد.

### 4- تغيير الألوان الرئيسية (الهوية) 🎨
1.  افتح ملف `tailwind.config.ts`.
2.  ابحث عن `colors` -> `primary` و `secondary`.
3.  هناك يمكنك وضع أكواد الألوان الجديدة (Hex Code) لتغيير ألوان الموقع بالكامل بلمسة واحدة.

---

**ملاحظة أخيرة:** هذا المشروع مصمم ليكون **سريعاً جداً** (SEO Friendly) ومتجاوباً مع الموبايل والتابلت بشكل ممتاز.

بالتوفيق في مشروعك! 🚀🚚
